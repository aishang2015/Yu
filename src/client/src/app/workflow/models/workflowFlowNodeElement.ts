export class WorkflowFlowNodeElement {
    defineId: string;       // 工作流定义id
    flowNodeId: string;     // 流程图节点id
    formElementId: string;  // 表单元素id
    isVisible: boolean;     // 是否可见
    isEditable: boolean;    // 是否可编辑
}